schema_version = "agentmesh-app.v0"
name = "adapter-evidence-envelope"
protocol_version = "2026-07-15"

[plugin]
logical_name = "agentmesh-adapter-evidence-envelope"

[schemas]
input = "schemas/adapter-evidence-envelope-input-v0.schema.json"
output = "schemas/adapter-evidence-envelope-compact-v0.schema.json"

[sidecar]
retention_class = "owner_local"
capture_plugin_stderr = false

[conformance]
cargo_package = "agentmesh-adapter-metadata-canonicalizer"
